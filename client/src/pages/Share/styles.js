const styles = theme => ({
  
  container: {
    display: 'flex',
    justifyContent: 'space-around',
    alignItems: 'center',
    backgroundColor: 'white',
    height: 700,
    paddingBottom: 200,
    paddingLeft: 100
  }
});

export default styles;
